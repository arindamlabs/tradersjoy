"""The read-only Streamlit dashboard for the paper account.

``app.py`` is the Streamlit script launched by ``tradersjoy dashboard``;
``data.py`` holds the pure, testable shaping helpers it renders.
"""
